# Copyright 2026 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: CUDA 13 Linux aarch64 -> Linux aarch64 build

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read
  pull-requests: read
  issues: read

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  # Don't cancel in-progress jobs for main branch
  cancel-in-progress: ${{ github.ref != 'main' }}

jobs:
  run-tests:
    name: "CUDA 13 Linux aarch64 -> Linux aarch64 build. Runner ${{ matrix.job_info.runner }}"
    defaults:
      run:
        # Explicitly set the shell to bash
        shell: bash
    strategy:
      matrix:
        job_info: [
          {
            runner: "linux-arm64-c4a-16",
            container: "us-docker.pkg.dev/ml-oss-artifacts-published/ml-public-container/ml-build-arm64:latest",
          },
        ]
    # Use self-hosted runners. For a Linux runner, a corresponding container image used for the
    # workflow is required.
    # See go/ml-github-actions/getting_started/using_runners.md#supported-runner-scale-set.
    runs-on: ${{ matrix.job_info.runner }}
    container:
      image: ${{ matrix.job_info.container }}
    steps:
      - uses: actions/checkout@v5
      - run: echo "NVCC building..."
      - run: |
          bazel build --config=build_cuda_with_nvcc \
            --config=cuda13 \
            --config=cuda_libraries_from_stubs \
            --repo_env=HERMETIC_CUDNN_VERSION=9.12.0 \
            //cc/tests/gpu:all

      - run: echo "NVCC Static CUDA building..."
      - run: |
          bazel build --config=build_cuda_with_nvcc \
            --config=cuda13 \
            --config=cuda_libraries_from_stubs \
            --@rules_ml_toolchain//common:link_cuda_static_libs=true \
            --repo_env=HERMETIC_CUDNN_VERSION=9.12.0 \
            //cc/tests/gpu:all

      - run: echo "NVCC Static CUDNN CUDA testing..."
      - run: |
          bazel build --config=build_cuda_with_nvcc \
            --config=cuda13 \
            --config=cuda_libraries_from_stubs \
            --@rules_ml_toolchain//common:link_cuda_static_libs=true \
            --@rules_ml_toolchain//common:link_cudnn_static_libs=true \
            --repo_env=HERMETIC_CUDNN_VERSION=9.12.0 \
            //cc/tests/gpu:cudnn_test


      - run: echo "NVCC Static NVRTC CUDA testing..."
      - run: |
          bazel build --config=build_cuda_with_nvcc \
            --config=cuda13 \
            --config=cuda_libraries_from_stubs \
            --@rules_ml_toolchain//common:link_cuda_static_libs=true \
            --@rules_ml_toolchain//common:link_nvrtc_static_libs=true \
            --repo_env=HERMETIC_CUDNN_VERSION=9.12.0 \
            //cc/tests/gpu:nvrtc_test

      - run: echo "Bzlmod. NVCC building..."
      - run: |
          bazel build --config=build_cuda_with_nvcc \
            --config=cuda13 \
            --config=cuda_libraries_from_stubs \
            --config=bzlmod \
            --repo_env=HERMETIC_CUDNN_VERSION=9.12.0 \
            //cc/tests/gpu:all
